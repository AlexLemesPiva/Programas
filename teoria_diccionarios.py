''' 
Diccionarios

d= {}
d = dict.()
d = {1:'uno', 2:'dos'}
d[valor] = texto  > retorna d = {valor: texto}
d.setdefault(5, [1, 2, 3, 4])
d.setdefault

d.get(valor) = consulta valor en esa posición
d.pop(valor) = eliminar elemento
d.popitem() = elimina tupla > va eliminando el ultimo elemento
d.keys() = devuelve las llaves
dict_values(): regresa los valores
d.items(): devuelve la tupla con los items
d.clear()
'''

